name: CI (API Surface)

on:
  push:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  api-surface:
    name: API surface (lite imports) (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        python-version: [ '3.10', '3.11', '3.12', '3.13' ]
    env:
      PYTHONPATH: src
      INSIGHTSPIKE_LITE_MODE: "1"
      INSIGHTSPIKE_MIN_IMPORT: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install minimal runtime deps
        run: |
          python -m pip install --upgrade pip
          pip install "numpy>=1.26.0,<3.0" "pydantic>=1.10,<3.0" \
            "networkx>=3.1" "pyyaml>=6.0" "typer>=0.9,<1.0" "scikit-learn>=1.3"

      - name: Quick import checks
        run: |
          python - << 'PY'
          # Basic public and package entrypoints should import in lite mode
          import importlib
          import sys
          ok = []
          for mod in [
            'insightspike',
            'insightspike.public',
            'insightspike.algorithms',
            'insightspike.implementations',
          ]:
            try:
              importlib.import_module(mod)
              ok.append(mod)
            except Exception as e:
              print(f'IMPORT FAIL: {mod}: {e}', file=sys.stderr)
              raise
          print('OK imports:', ok)
          PY

      - name: Public API smoke
        run: |
          python - << 'PY'
          from insightspike.public import create_agent, get_config_summary
          agent = create_agent()  # mock LLM in lite mode
          res = agent.process_question('geDIG quick check')
          print('agent success:', bool(res))
          summ = get_config_summary()
          print('summary keys:', len(summ))
          PY
