name: Release (tag)

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  gh-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          prerelease: false
          # Paths must exist in the tagged tree; keep in sync with docs/paper/<latest>/
          files: |
            docs/paper/v6.1/geDIG_onegauge_improved_v6_1.pdf
            docs/paper/v6.1/arxiv_en/geDIG_onegauge_improved_v6_1_en.pdf
            docs/paper/data/maze_25x25_*_s500.json
          body_path: docs/releases/RELEASE_NOTES_v0.1.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

