# 2025-11-09 — Exp II–IV (exp2to4_lite) Refresh Notes

## What changed
- Re-ran `exp23_paper.yaml` (Exp II–III lite) after updating the
  geDIG gate to `theta_ag=2.0`, `theta_dg=0.05` (calibrated on val to
  target AG≈0.08, DG≈0.04).
- Latest pipeline outputs:
  - `experiments/exp2to4_lite/results/exp23_paper_20251109_064138.json`
  - Summary: `exp23_summary_20251109_064138.{json,csv}`
  - Metrics: geDIG (PER≈0.421, Acc≈0.374, FMR≈0.626, P50=240 ms,
    avg_steps≈2.88, dg_rate≈0.124).
- Static RAG baselines remain unchanged (PER≈0.172, Acc=0, P50=160 ms).

## Paper assets updated
- `docs/paper/figures/exp23_summary_table.tex` — refreshed summary table
  (P50/P95 values, s_PSZ, etc.).
- `docs/paper/geDIG_onegauge_improved_v4_en.tex` and `_v4.tex` —
  narrative text (“Key numbers” and config paragraphs) now cite
  avg_steps≈2.88 and gate pair (2.0, 0.05).
- Ablation table already matched the latest numbers (no change).

## Next steps / notes
- PSZ band still not fully reached (`psz_inside=False`); to tighten
  `dg_rate` further, consider expanding the calibration grid (e.g.,
  theta_dg∈[0.02,0.08]) or adding a PSZ-specific run.
- Static RAG baselines could be tuned (top_k, BM25 weight) if we want to
  show slight improvements while keeping the “static performance is not
  degraded” story.
